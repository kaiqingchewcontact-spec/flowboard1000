# AGENTS.md

## Cursor Cloud specific instructions

This repository (`flowboard1000`) is currently an empty/newly initialized project with only a `README.md`. There are no source files, dependencies, build systems, or services to run.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `Makefile`, or similar exists yet.
- **No services to start** — no application code, Docker configuration, or dev server setup exists.
- **No tests or lint** — no test framework or linting configuration is present.

When source code is added to this repository, this section should be updated with:
1. How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
2. How to run the dev server(s).
3. How to run tests and linting.
4. Any non-obvious caveats discovered during setup.
